import java.util.Scanner;

class Shape {
    public double circle(double radius) {
        return Math.PI * radius * radius;
    }

    public double rectangle(double length, double width) {
        return length * width;
    }

    public double triangle(double base, double height) {
        return (base * height) / 2;
    }

    public void calculateCircle(Scanner scanner) {
        System.out.print("Enter radius: ");
        double radius = scanner.nextDouble();
        double area = circle(radius);
        System.out.println("Area of Circle: " + area);
    }

    public void calculateRectangle(Scanner scanner) {
        System.out.print("Enter length: ");
        double length = scanner.nextDouble();
        System.out.print("Enter width: ");
        double width = scanner.nextDouble();
        double area = rectangle(length, width);
        System.out.println("Area of Rectangle: " + area);
    }

    public void calculateTriangle(Scanner scanner) {
        System.out.print("Enter base: ");
        double base = scanner.nextDouble();
        System.out.print("Enter height: ");
        double height = scanner.nextDouble();
        double area = triangle(base, height);
        System.out.println("Area of Triangle: " + area);
    }
}

public class Main {
    public static void main(String[] args) {
        Shape shape = new Shape();
        Scanner scanner = new Scanner(System.in);

        while (true) {
            System.out.println("Select:\n1. Circle, 2. Rectangle. 3. Triangle\n");
            System.out.print("Enter your choice: ");

            int choice = scanner.nextInt();

            switch (choice) {
                case 1:
                    shape.calculateCircle(scanner);
                    break;
                case 2:
                    shape.calculateRectangle(scanner);
                    break;
                case 3:
                    shape.calculateTriangle(scanner);
                    break;
                case 4:
                    System.out.println("Exiting...");
                    scanner.close();
                    return;
                default:
                    System.out.println("Invalid choice. Try again.");
            }
        }
    }
}
